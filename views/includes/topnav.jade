.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') 
        i.fa.fa-home
    #navbar.collapse.navbar-collapse
      ul.nav.navbar-nav
        if (currentUser)
          if (currentUser._id == "5849126ff2410d3594f6c838")    
            li
              a(href='/users/#{currentUser._id}') Users
      ul.nav.navbar-nav.navbar-right
        if (currentUser) 
          li
            a(href='/profile/#{currentUser._id}') Profile
          li
            a(href='/signout/#{currentUser._id}') Signout
          li
            a(href='/hosting/#{currentUser._id}') Hosting
          li
            a(href='/room_list/#{currentUser._id}') Room_list
          li
            a(href='/users/#{currentUser._id}/edit') Edit
          li
            a(href='/users/#{currentUser._id}?_method=DELETE') ID_Delete
        else
          li
            button.btn.btn-default(type='button', data-toggle='modal', data-target='#signin')  Signin
          li
            button.btn.btn-default(type='button', data-toggle='modal', data-target='#signup')  Signup
          
