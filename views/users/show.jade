extends ../layout

block content
  .container
    h1.page-header 회원 정보
          
    ul
      li 이름: #{user.name}
      li 이메일: #{user.email}
    hr
    p
      a.btn.btn-default(href='javascript:window.history.back();') Back
